import React from 'react';
import config from '../../../config';

const ReactComponent = () => {
  return <img style={{ maxWidth: 1200 }} src={config.reactPic[0]} />;
};

export default ReactComponent;
